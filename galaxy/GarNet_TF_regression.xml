<tool id="TF_regression" name="TF_regression" version="0.2.20">
  <description>Integrate Epigenetics and Transcriptomics data</description>
  <requirements>
    <requirement type="package" version="0.2.20">garnet</requirement>
  </requirements>
  <command>
    GarNet
        -p $peaks_file
        -m $motifs_file
        -g $known_genes_file
        -x $xref_file
        -e $expression_file
        #if $upstream_window
          --up $upstream_window
        #end if
        #if $downstream_window
          --down $downstream_window
        #end if
        $tss
        -o OUTPUTDIR
  </command>
  <inputs>
    <!-- data -->
    <param name="peaks_file" type="data" type="bed" optional="true" label="" help="BED file containing epigenetic regions of interest" />
    <param name="motifs_file" type="data" type="bed" optional="true" label="" help="BED file containing locations and scores of TF motifs" />
    <param name="known_genes_file" type="data" format="tsv" optional="true" label="" help="File containing locations of known genes in the reference genome (i.e. from UCSC Annotation Database)" />
    <param name="xref_file" type="data" format="tsv" optional="true" label="" help="File containing information about known genes (i.e. from UCSC Annotation Database)" />
    <param name="expression_file" type="data" format="tsv" optional="true" label="" help="File Containing results of RNA-Seq experiment" />
    <!-- params -->
    <param name="upstream_window" type="integer" optional="true" value="2000" label="Upstream Window" help="Window width in base pairs to consider upstream region." />
    <param name="downstream_window" type="integer" optional="true" value="2000" label="Downstream Window" help="Window width in base pairs to consider downstream region." />
    <param name="tss" type="boolean" truevalue="--tss" falsevalue="" checked="false" help="Calculate downstream window from transcription start site instead of transcription end site." />
  </inputs>

  <outputs>
    <data name="" from_work_dir="output_dir/output_figures/-log10_P-value.png" hidden="false" />

  </outputs>
  <help>

  </help>
  <citations><citation type="doi"></citation></citations>
</tool>
